import { useState } from 'react';

// Deprecated: Hard-coded admin login removed. Keep component returning null to avoid crashes if referenced.
const LoginModal = ({ onClose, onLogin }) => {
  return null;
};

export default LoginModal;